#include "pch.h"
#include <iostream>
#include <string>
#include <string.h>
#include <fstream>


using namespace std;





class repositor
{
private:
	string *library; // названия файлов
	char *text_user; // текст введенный польвзвоталем
	int number_file;
public:

	void to_change_library();
	
	repositor();
	~repositor();





};



void repositor::to_change_library()
{
	int n,count;
	string *time_mas;
	cout << "add : 1" << endl;
	cout << "delete all and add : 2" << endl;
	cin >> n;
	switch (n)
	{
	case 1:
		
		time_mas = new string[number_file];
		for (int i = 0; i < number_file; i++)
		{
			time_mas[i] = library[i];
		}
		
		delete library;
		
		number_file = number_file + 1;
		library = new string[number_file];
		for (int i = 0; i < number_file; i++)
		{
			library[i] = time_mas[i];
		}

		cout << "Input your file" << endl;
		cin>>library[number_file];
		delete time_mas;
		break;
	case 2:
		
		cout << "input count" << endl;
		cin >> number_file;
		if (library == NULL)
		{
			library = new string[number_file];
			for (int i = 0; i < number_file; i++)
			{
				cin >> library[i];
			}
		
		}
		else
		{
			delete library;
			library = new string[number_file];
			for (int i = 0; i < number_file; i++)
			{
				cin >> library[i];
			}
		}



		break;
	default:
		
		cout << "Eror" << endl;
		
		break;
	}

}

repositor::repositor()
{
}



repositor::~repositor()
{
	delete library;
}








int main()
{
	//repositor a;
	string file;
	char *str;
	str = new char[1000];
	//cin.getline(str, 100);
	//cin >> str;
	//cout << str;
	file = "file.txt";

	char simvol;

	ifstream a;
	a.open(file);
	int count = 0;
	while (a.get(simvol))
	{
		str[count] = simvol;
		count++;
		if (count ==1000)
		{
			cout << "sadas" << endl;
			break;
		}
		
	}

	count = 100;
	for (int i = 0; i < 1000; i++)
	{
		cout << str[i];
		if (i = count)
		{
			count = count + 100;
			cout << endl;
		}
	}





	//char *str_input,**str_file;
	//int count;
	//str_file = new char*[40];
	//for (int i = 0; i < 40; i++)
	//{
	//	str_file[i] = new char[100];
	//}
	//for (int i = 0; i < 40; i++)
	//{
	//	for (int j = 0; j < 100; j++)
	//	{
	//		str_file[i][j] = '0';
	//	}
	//	
	//}

	//cout << "Input text" << endl;
	//cin >> str_input;
	//
	//cout << "Input count files with text" << endl;
	//cin >> count;
	//for (int i = 0; i < count; i++)
	//{
	//	cout << "Input name of file with text :" << count << endl;
	//	cin >> str_file[i];
	//}
	//
	//ifstream a;

	return 0;
}
